const mainContent = require('./mainContent');
// const navigationContent = require('./navigationContent');
const systemContent = require('./systemContent');

module.exports = Object.assign({}, mainContent, systemContent);